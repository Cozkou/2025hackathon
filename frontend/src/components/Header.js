import React from 'react';
import './Header.css'; // Import a CSS file for styling

const Header = () => {
    return (
        <header className="header">
            <h1>Welcome to EduFlash</h1>
            <p className="slogan">Empower Your Learning with AI</p>
        </header>
    );
};

export default Header; // Ensure this is a default export
